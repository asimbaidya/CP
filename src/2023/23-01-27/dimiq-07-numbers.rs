fn main() {
    let mut input = String::new();

    std::io::stdin().read_line(&mut input);

    let count: usize = input.trim().parse().unwrap();

    for _ in 0..count {
        let mut input = String::new();
        std::io::stdin().read_line(&mut input);
        // todo
    }
}
